AETHERIS WEBSITE — TEST DOMAIN MIGRATION NOTES

Primary website: https://aetherisbot.com
Compatibility website: https://aetherisbot.club
Both apex domains and their www aliases remain on the existing Worker: aetheris.

Primary API: https://api.aetherisbot.com
Compatibility API: https://api.aetherisbot.club
Both API hostnames remain on Tunnel AetherisBot, forwarding to http://127.0.0.1:8787.
Do not delete, replace or redirect away the old API hostname: older desktops still use it.

This workspace contains TEST source changes only. No deployment is authorized here.
After separate manual verification and deployment authorization:
- Keep all HTML pages, assets/, _headers, robots.txt and sitemap.xml together.
- Update the existing Worker asset deployment; do not create a replacement Pages project or remove existing routes.
- Ensure the Worker actually applies the CSP in _headers: both API origins must be allowed.
- The backend /health CORS policy must allow both apex and www website origins exactly.
- Pairing URLs must remain .club for old clients connecting through api.aetherisbot.club, and prefer .com for api.aetherisbot.com.
- TWITCH_REDIRECT_URI is server-environment configuration, not a website link. Preserve the currently registered callback until a separate coordinated Twitch application/environment migration is approved. Keep the old callback route accessible.
- Do not alter EventSub callback configuration, credentials or tunnel routing as part of this source-only change.

Validate all website pages and health fetches on both domains, existing/fresh desktop authentication, reconnects and legacy-client pairing before a production migration.
The homepage player remains an animated fictional demo; it does not display a user's real song or identity.
